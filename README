Leaflet Map with Vector Data
This project is a simple web map created using Leaflet.js.
The map displays several types of vector data, including:
Point (Marker): shows a location on the map.
Line (Polyline): represents a path or route.
Polygon: represents an area or region.
GeoJSON Layer: vector data in GeoJSON format (points, lines, and polygons).

How to Run
Download or clone the project folder.
Open the file map.html in your browser (no server required).
The map will load with OpenStreetMap basemap and display all vector data.

Technologies Used
Leaflet.js
 for interactive maps.
OpenStreetMap tiles as the basemap.
HTML + JavaScript.

Screenshot
